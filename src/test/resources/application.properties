# Configuracion de pruebas (valores dummy, sin conexion real a Azure/AWS)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop

spring.security.oauth2.resourceserver.jwt.issuer-uri=https://login.microsoftonline.com/test-tenant/v2.0
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://test.b2clogin.com/test/discovery/v2.0/keys
azure.b2c.audience=test-client-id
azure.b2c.role-claim=extension_rol

aws.s3.bucket-name=test-bucket
aws.s3.region=us-east-1
aws.access-key=
aws.secret-key=
aws.session-token=
